#include <stdio.h>

int main() {
    int a = 8;
    int t = 5;
    int luassegitiga = (a * t) / 2;

    // menampilkan data yang dimasukan
    printf("luas segitiga: %i", luassegitiga);

    return 0;
}